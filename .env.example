# Database Configuration
# Set DB_TYPE to 'postgresql' for production or 'sqlite' for development/testing
DB_TYPE=postgresql

# PostgreSQL Configuration (required when DB_TYPE=postgresql)
DB_HOST=your-gcp-postgres-ip
DB_PORT=5432
DB_NAME=option_strategies
DB_USER=optcom-user
DB_PASSWORD=your-secure-password

# SQLite Configuration (used when DB_TYPE=sqlite or as fallback)
SQLITE_DB_PATH=./database/option_strategies.db

# Optional: Cloud SQL Proxy configuration
# If using Cloud SQL Proxy, set DB_HOST=127.0.0.1
INSTANCE_CONNECTION_NAME=your-project:region:instance-name